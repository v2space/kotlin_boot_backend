spring:
  application:
    name: kotlin_boot_backend
  datasource:
    url: jdbc:postgresql://localhost:5432/v2space
    username: v2space
    password: v2space1234
    driver-class-name: org.postgresql.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQL8Dialect
